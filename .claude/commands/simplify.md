Review and simplify: $ARGUMENTS

Checklist:
1. Duplication — extract shared logic
2. Naming — clear, descriptive names
3. Complexity — remove over-engineering
4. Dead code — remove unused imports/functions
5. Comments — remove "what" comments, keep "why"

Rules: all tests must stay green. Run `just verify` after simplifying.
